<div class="py-20 px-3">
  <div class="text-center">
    <h2 class="font-bold text-2xl md:text-3xl lg:text-4xl mb-6 ">ゲーム一覧</h2>
    <p class="my-5">学びたいゲームを選んでください<br>＊日本で人気のシューティングゲームのみ対応しています</p>
  </div>
  <div class="text-black flex justify-center my-3">
    <div data-controller="autocomplete" data-autocomplete-url-value="/autocomplete">
      <%= form_tag games_path, method: :get, class: "mb-4" do %>
        <div class="flex space-x-2">
          <input type="text" name="title" value="<%= params[:title] %>" placeholder="検索" class="w-full rounded-full py-2 pl-4 pr-10 focus:ring-2 focus:ring-blue-500 focus:border-transparent" data-autocomplete-target="input">
          <%= button_tag type: :submit do %>
            <i class="fas fa-magnifying-glass pl-1"></i>
          <% end %>
        </div>
      <% end %>
      <ul data-autocomplete-target="results"></ul>
    </div>
  </div>
  <div class="px-10 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
    <% @games.each do |game| %>
      <%= link_to(game_path(game.title)) do %>
        <div class="flex items-center mb-4 border rounded-lg p-4 hover:scale-105 transition duration-300 shadow-md">
          <%= image_tag game.logo_url, class: "h-24 w-24 object-contain mr-4" %>
          <div>
            <%= content_tag :div, game.title, class: "font-bold mb-2 text-lg" %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
  <%= paginate @games %>
</div>